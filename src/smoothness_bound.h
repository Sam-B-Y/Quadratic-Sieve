#ifndef SMOOTHNESS_BOUND_H
#define SMOOTHNESS_BOUND_H

#include <gmpxx.h>

// Smoothness bound for the given composite number n
unsigned long chooseSmoothnessBound(const mpz_class &n);

#endif // SMOOTHNESS_BOUND_H
